# Migrating from Jest

Rjest is not ready for production migration yet. The current executable provides
native configuration normalization and discovery through `--showConfig` and
`--listTests`. Follow the compatibility matrix rather than assuming an unlisted
Jest behavior works.
